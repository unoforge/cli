import { defineConfig } from "rasengan";
import { rasengan } from "rasengan/plugin";
import UnoCSS from "unocss/vite";

export default defineConfig({
  vite: {
    plugins: [rasengan(), UnoCSS()],
  },
});
